kind = "web"
previewPath = "/"
title = "ALT — Agency Automation Platform"
version = "1.0.0"
id = "artifacts/alt"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18928

[services.development]
run = "pnpm --filter @workspace/alt run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/alt", "run", "build" ]
publicDir = "artifacts/alt/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18928"
BASE_PATH = "/"
